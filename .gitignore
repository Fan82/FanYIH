# 忽略 node_modules 資料夾
node_modules/

# 忽略編譯輸出的文件
dist/
build/

# 忽略 IDE 配置文件
.vscode/

# 忽略操作系統生成的文件
.DS_Store

# 如果是 Monorepo（多專案）結構，確保不會忽略專案內的 package.json
# **錯誤**：不應該忽略整個專案的 package.json
# /package.json  
# /package-lock.json  

# 若外層 `package.json` 只是錯誤建立的，直接刪除就好，不用加進 `.gitignore`

# 針對 npm / yarn / pnpm 可能產生的檔案
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
